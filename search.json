---
---
[
  {% assign published_recipes = site.recipes | where_exp: "r", "r.status != 'draft' and r.status != 'planned'" %}
  {% for post in published_recipes %}
    {
    
	    "title"    : "{{ post.title | escape }}",
	    "reviewed"  : {% if post.reviewed %}true{% else %}false{% endif %},
	    "tile_class" : "{% if post.image %}has-image{% else %}no-image{% endif %}",
	    "reviewed_badge"   : "{% if post.reviewed %}<span class=\"reviewed-badge reviewed card-badge\">Reviewed</span>{% endif %}",
      {% if post.ingredients %}"ingredients"    : "{% for ingredient in post.ingredients %}{{ ingredient | escape }}, {% endfor %}",{% endif %}
      {% if post.tags %}"tags"    : "{% for tag in post.tags %}{{ tag | escape }} {% endfor %}",{% endif %}
	{% if post.image contains 'http' %}
	  "image": "{{ post.image }}",
	{% elsif post.image %}
	  "image": "/images/{{ post.image }}",
	{% else %}
	  "image": "",
	{% endif %}
	"url"      : "{{ post.url }}"
      
    } {% unless forloop.last %},{% endunless %}
  {% endfor %}
]
