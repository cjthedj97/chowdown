---
layout: null
---
{% assign recipeCounter = 1 %}
{% assign tagCounter = 1 %}
{% assign totalRecipes = site.recipes | size %}
{% assign totalTags = site.recipes.tags | size %}

{
  "recipeCount": {{ totalRecipes }},
  "tagCount": {{ totalTags }},
  "recipes": [
    {% for post in site.recipes %}
        {
            "title": "{{ post.title }}",
            "tags": [{% for tag in recipes.tags %}"{{ tag }}"{% if forloop.last %}{% else %},{% endif %}{% endfor %}]
        }{% if recipeCounter == totalRecipes %}{% else %},{% endif %}
    {% assign recipeCounter = recipeCounter | plus: 1 %}
    {% endfor %}
  ]
}
